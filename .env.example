DB_NAME=example_db
DB_USER=example_admin
DB_PASSWORD=example_password
DB_PORT=5432

# JWT Security
# Generate a long and random string (e.g., at least 64 characters) for security.
JWT_SECRET=your_super_secret_jwt_key_that_should_be_long_and_complex